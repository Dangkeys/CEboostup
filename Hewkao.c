#include<stdio.h>
int num;
int main()
{
    scanf("%d", &num);

    if(num < 99 && num > 1)
    {
        for(int i = 1; i <= num; i++)
        {
            char answer[] = "*" * i;
            printf("%s", )
        }
    } else {
        printf("ERROR");
    }
}